Para utilizarmos o php na web sem a necessidade de aplicativos de 3° (xampp, wamp etc) podemos usar o servidor local que o próprio PHP nos oferece: Built-in web server

para rodarmos temos que executar o seguinte código no terminal:

php -S localhost:{port} (seja ela a porta 8000, 8080, 800, só precisar estar livre no computador)